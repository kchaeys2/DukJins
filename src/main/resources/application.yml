spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dukjins?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: DBcodus1206^^
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    org.hibernate.sql: debug  # ?? logger? ?? ??? ????